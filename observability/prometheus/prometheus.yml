global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'local'

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Example: FastAPI application with /metrics endpoint
  # Uncomment and modify for your application
  # - job_name: 'my-app'
  #   scrape_interval: 10s
  #   static_configs:
  #     - targets: ['host.docker.internal:8000']
  #       labels:
  #         app: 'my-app'
  #         environment: 'development'
